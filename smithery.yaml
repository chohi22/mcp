version: 1
name: procurement-server
description: Simple procurement server

build:
  dockerfile: Dockerfile

deploy:
  type: service
  port: 5000

environment:
  - KOREA_API_KEY
  - SUPABASE_URL  
  - SUPABASE_KEY

resources:
  memory: 512Mi
  cpu: 256m